node_modules/
_site/
.cache/
.DS_Store
*.log

# Build artifacts — regenerated by `npm run og` on every build
src/images/og/
scripts/.font-cache/

# Codex agent worktrees — a stale full checkout lives here. Local-only
# .git/info/exclude hid it from git status but not from a clone or a grep.
.codex/
