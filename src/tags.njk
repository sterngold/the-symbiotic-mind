---
pagination:
  data: collections.tags
  size: 1
  alias: tag
permalink: "/tags/{{ tag }}/index.html"
eleventyComputed:
  title: "#{{ tag }}"
  description: "Essays tagged #{{ tag }} on The Symbiotic Mind — behavioral science and practitioner perspectives on human-AI symbiosis."
ogImage: "/images/og/tags.png"
layout: layouts/base.njk
---

{% block head %}
{% set crumbs = [
  { name: 'Home', url: '/' },
  { name: 'Topics', url: '/tags/' },
  { name: '#' + tag, url: page.url }
] %}
{% include "partials/breadcrumbs.njk" %}
{% endblock %}

<div class="posts-header">
  <h1>#{{ tag }}</h1>
</div>

<ul class="post-list">
{% for post in collections.posts %}
  {% if post.data.tags and (tag in post.data.tags) %}
  <li>
    <div class="post-meta">{{ post.date | postDate }} &middot; {{ authors[post.data.author].name }}</div>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    {% if post.data.description %}<p class="post-excerpt">{{ post.data.description }}</p>{% endif %}
  </li>
  {% endif %}
{% endfor %}
</ul>
